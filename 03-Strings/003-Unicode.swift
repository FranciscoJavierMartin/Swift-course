let a = "a" // U+0061
let a_unicode = "\u{61}"
print(a_unicode)

// It is possible combine multiple unicode character to create
// new characters. Useful to show icons and non latin characters.
// Some characters like accent mark are not count on the string.count